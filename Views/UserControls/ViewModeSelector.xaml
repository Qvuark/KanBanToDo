<UserControl x:Class="KanbanToDo.Views.UserControls.ViewModeSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <Border Style="{StaticResource SectionBorder}">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Доска задач" Style="{StaticResource SectionHeader}"/>
            <ComboBox ItemsSource="{Binding ViewModes}" SelectedItem="{Binding CurrentViewMode}"
                  Margin="20,0,0,0" Width="40">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
    </Border>
</UserControl>
